# Web framework
fastapi==0.121.2
uvicorn[standard]==0.38.0

# Authentication
argon2-cffi==25.1.0
email-validator==2.3.0

# Data & validation
pydantic==2.12.4
pydantic-settings==2.12.0

# Database
sqlalchemy==2.0.44
alembic==1.17.0
psycopg[binary]==3.2.10
pgvector==0.4.1

# LLM provider (routing service) — real calls only when ANTHROPIC_API_KEY is set
anthropic==0.75.0

# Embedding provider (optional real embeddings) — only used when EMBEDDING_PROVIDER=openai
openai==2.9.0

# Utilities
python-dotenv==1.2.1
numpy==2.3.5

# Testing
pytest==8.4.2
pytest-asyncio==1.3.0
httpx==0.28.1
