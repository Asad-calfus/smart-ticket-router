# Web framework
fastapi==0.121.2
uvicorn[standard]==0.38.0

# Authentication
argon2-cffi==25.1.0
email-validator==2.3.0

# Data & validation
pydantic==2.12.4
pydantic-settings==2.12.0

# Database
sqlalchemy==2.0.44
alembic==1.17.0
psycopg[binary]==3.2.10
pgvector==0.4.1

# LLM providers (routing service) — real calls only when the matching API key is set.
# anthropic: used when LLM_PROVIDER=anthropic. openai: used when LLM_PROVIDER=openai
# and/or EMBEDDING_PROVIDER=openai (same package serves both).
anthropic==0.75.0
openai==2.9.0

# Encrypts personal LLM API keys at rest (per-user LLM settings).
cryptography==49.0.0

# Utilities
python-dotenv==1.2.1
numpy==2.3.5

# Testing
pytest==8.4.2
pytest-asyncio==1.3.0
httpx==0.28.1
